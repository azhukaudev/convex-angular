# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

.nx/self-healing
pnpm-lock.yaml

# Convex codegen output — `convex dev` rewrites these in its own style, so
# formatting them just churns the diff on every regeneration.
apps/frontend/src/convex/_generated/
