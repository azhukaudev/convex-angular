## Environment

- The default branch in this repo is `main`.
- Use `pnpm` for package management.

## General Practices

- Preserve existing code comments unless the change explicitly requires updating or removing them.
- Add explanatory comments only for non-obvious logic or behavior contracts; avoid comments that just restate the code.
